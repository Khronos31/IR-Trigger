{
    "config": {
        "abort": {
            "single_instance_allowed": "Already configured. Only a single configuration is possible."
        },
        "step": {
            "user": {
                "description": "Set up IR-Trigger?",
                "title": "IR-Trigger setup"
            }
        }
    },
    "title": "IR-Trigger"
}
